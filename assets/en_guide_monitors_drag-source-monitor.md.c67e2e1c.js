import{_ as e,c as t,o,Q as d}from"./app.84d2bec0.js";const m='{"title":"DragSourceMonitor","description":"","frontmatter":{},"headers":[{"level":2,"title":"method","slug":"method"}],"relativePath":"en/guide/monitors/drag-source-monitor.md"}',r={},c=d('<h1 id="dragsourcemonitor" tabindex="-1">DragSourceMonitor <a class="header-anchor" href="#dragsourcemonitor" aria-hidden="true">#</a></h1><p><code>DragSourceMonitor</code> is the object passed to <code>DragSource</code> drag source. It provides methods that allow you to get some state information about the drag source. The specific drag source bound to this monitor is referred to below as the monitor&#39;s owner.</p><h2 id="method" tabindex="-1">method <a class="header-anchor" href="#method" aria-hidden="true">#</a></h2><table><thead><tr><th>method name</th><th>type</th><th>description</th></tr></thead><tbody><tr><td><code>canDrag()</code></td><td><code>() =&gt; boolean</code></td><td><code>true</code> if no drag operation is in progress and the <code>canDrag()</code> method of the owner <code>useDrag</code> returns <code>true</code> or <code>undefined</code> <code>, otherwise return </code>false`.</td></tr><tr><td><code>isDragging()</code></td><td><code>() =&gt; boolean</code></td><td>If a drag operation is in progress and the owner is being dragged, or the <code>isDragging()</code> method of <code>useDrag</code> returns <code>true</code>, this method will Will return <code>true</code>, otherwise return <code>false</code>.</td></tr><tr><td><code>getItemType()</code></td><td><code>() =&gt; (string | null)</code></td><td>Returns the type <code>type</code> that identifies the currently dragged item. Returns <code>null</code> if no item was dragged.</td></tr><tr><td><code>getItem()</code></td><td><code>() =&gt; (object | null)</code></td><td>Returns a plain object representing the currently dragged item. Each drag source must be specified by specifying it from its <code>item</code> property. Returns <code>null</code> if no item was dragged.</td></tr><tr><td><code>getDropResult()</code></td><td><code>() =&gt; (object | null)</code></td><td>Returns a plain object representing the drop result of the last record. <br>Drop targets can be specified by returning an object from their <code>drop()</code> method. <br>When the drop target is a nested target, any parent object that explicitly returns its own result from <code>drop()</code> overrides the <code>drop</code> result previously set by the child object. <br><strong>It can only be called inside <code>end()</code></strong>, if called outside <code>end()</code> it will return <code>null</code>.</td></tr><tr><td><code>didDrop()</code></td><td><code>() =&gt; boolean</code></td><td>Returns <code>true</code> if a drop target has handled the drop event, <code>false</code> otherwise. <code>didDrop()</code> returns <code>true</code> even if the target does not return a drop result. <br>It can be used in <code>end()</code> to check if a drop target has handled the drag. Returns <code>false</code> if called outside <code>end()</code>.</td></tr><tr><td><code>getInitialClientOffset()</code></td><td><code>() =&gt; { x: number, y: number }</code></td><td>Returns the initial position <code>{x, y}</code> of the mouse pointer relative to the client at the start of the current drag. <br>If no item was dragged, <code>null</code> is returned.</td></tr><tr><td><code>getInitialSourceClientOffset()</code></td><td><code>() =&gt; { x: number, y: number }</code></td><td>Returns the initial position of the root DOM node of the drag source relative to the client when the current drag starts <code>{x, y}</code> . <br>If no item was dragged, <code>null</code> is returned.</td></tr><tr><td><code>getClientOffset()</code></td><td><code>() =&gt; { x: number, y: number }</code></td><td>Returns the offset of the mouse pointer relative to the client of the drag start point <code>{x, y}</code>. <br>If no item was dragged, <code>null</code> is returned.</td></tr><tr><td><code>getDifferenceFromInitialOffset()</code></td><td><code>() =&gt; { x: number, y: number }</code></td><td>Returns the offset of the mouse pointer in the current drag relative to the mouse pointer position when the drag started <code>{x, y} </code>. <br>If no item was dragged, <code>null</code> is returned.</td></tr><tr><td><code>getSourceClientOffset()</code></td><td><code>() =&gt; { x: number, y: number }</code></td><td>Returns the offset <code>{x, y}</code> of the root DOM node of the current drag source relative to the client. <br>If no item was dragged, <code>null</code> is returned.</td></tr><tr><td>Note, if you need to get the <strong>real-time trajectory</strong> (real-time Offset) of the mouse, you should pay attention to the callbacks in DropTargetMonitor, such as hover</td><td></td><td></td></tr></tbody></table>',4),n=[c];function a(i,s,u,h,l,g){return o(),t("div",null,n)}var p=e(r,[["render",a]]);export{m as __pageData,p as default};
